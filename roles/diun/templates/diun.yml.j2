watch:
  workers: {{ diun_workers }}
  schedule: "{{ diun_schedule }}"
  firstCheckNotif: {{ diun_first_check_notify }}
  compareDigest: {{ diun_compare_digest }}
providers:
  docker:
    watchByDefault: true
    watchStopped: true
notif:
  {{ diun_notifier }}
